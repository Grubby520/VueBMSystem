1：可选
2：推荐
3：强制

项目约定
css 文件名 xx_xx   名字去url后面的值（2）
js  文件名 xx_xx   名字去url后面的值（2）
    lib 放第三方库（2）
    utility 放自己的插件（2）

chart 图标的自定义属性 尽量采用option 覆盖的方式（2）

列表类型的vm 建议id 跟vm的名字一样 方便寻找（2）

用异步的方式加载模块（1）

文件注释添加上作者

编程习惯
css ui原子组件命名规范  eg input(/input-default) , input-large, input-small, input-mid (1)
js  构造函数首字母大写（2）
    常亮 全大写  QQ_RR
文件名  dd_xx.js 下划线（2）
文件夹名称 xxxx-xx（2）

tab 兩個空格（2）


vue 编码规范
组件名称 WMap 首字母大写（2）

-----------------------------------------------------------------------

环境：
node > 8.0
vue cli 3.0
node 版本管理工具 nvm

环境变量配置：
.env.development

已支持的特性：
code split

待支持的特性：
dll
文件名ID固定
mockserver

-------------------------------------------------------------------------
loading

表单、弹框、按钮、下拉框 、表格、分页、(唐铨晟)

侧边栏、头、图标、top5、面包屑 （王雷）

（蒋亮亮）
-------------------------------------------------------------------------

项目结构约定
public:不需要编译的第三方文件
src:
  |--assets
      |--js
          |--components  自己写的可复用的组件
          |--lib  需要编译的第三方插件
          |--uti~ 与项目无关的工具js
      |--scss
          |--components  自己写的可复用的样式
          |--module  比components更复杂的定制样式
      |--components
          |--common 公共组件（超过一个文件的组件，用文件夹包起来）
          |--mixins
      |--views  具体的页面
          |--xx 按照路由进行文件创建